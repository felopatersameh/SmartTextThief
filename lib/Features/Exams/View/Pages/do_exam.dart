import 'package:flutter/material.dart';
    
class DoExam extends StatelessWidget {

  const DoExam({ super.key });
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}