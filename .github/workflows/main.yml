name: version 1.1.0

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.6'
    
    - name: Install dependencies
      run: flutter pub get
    
    - name: Run tests
      run: flutter test
    
    - name: Analyze code
      run: flutter analyze

  build_android:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.6'
    
    - name: Install dependencies
      run: flutter pub get
    
    - name: Build Android APK
      run: flutter build apk --release --dart-define=APP_ENV=production
    
    # - name: Build Android App Bundle
    #   run: flutter build appbundle --release --dart-define=APP_ENV=production
    
    - name: Upload Android Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: android-builds
        path: |
          build/app/outputs/flutter-apk/app-release.apk
 #          build/app/outputs/bundle/release/app-release.aab
  # build_ios_appetize:
  #   name: 🍎 Build iOS for Appetize
  #   runs-on: macos-latest
    
  #   steps:
  #   - name: 🔄 Checkout code
  #     uses: actions/checkout@v4
    
  #   - name: ⚙️ Setup Flutter
  #     uses: subosito/flutter-action@v2
  #     with:
  #       flutter-version: '3.35.6'
    
  #   - name: 📦 Install dependencies
  #     run: |
  #       flutter pub get
  #       cd ios
  #       pod install
    
  #   - name: 🏗️ Build iOS for Simulator
  #     run: flutter build ios --simulator --debug --no-codesign
    
  #   - name: 📦 Create iOS App Zip
  #     run: |
  #       cd build/ios/iphonesimulator
  #       zip -r app.zip Runner.app
    
  #   - name: 📤 Upload to Appetize
  #     run: |
  #       curl "https://api.appetize.io/v1/apps" \
  #         -F "file=@build/ios/iphonesimulator/app.zip" \
  #         -F "platform=ios" \
  #         -F "token=tok_7ucic4hz3p4ubtnufyjc6t6mzm"
    
  #   - name: 🔗 Get App URL
  #     run: |
  #       echo "✅ iOS App uploaded to Appetize!"
  #       echo "🌐 Check your dashboard: https://appetize.io/dashboard"